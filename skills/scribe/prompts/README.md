# Scribe prompts

Phase 2 home for LLM extraction prompts. MVP scribe is heuristic-only
(regex + frontmatter parsing); when an LLM client is wired in
(`ffs-config`-supplied), the prompts here will guide structured
extraction. Today this directory is reserved so the `SKILL.md`-shaped
bundle is shape-complete.
